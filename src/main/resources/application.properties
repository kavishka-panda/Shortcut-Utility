# Application Update Configuration
# This file contains settings for the auto-update feature

# Current application version (must match GitHub release tag format)
app.version=v1.0.0

# GitHub repository information
github.owner=kavishka-panda
github.repo=Shortcut-Utility

# Update check settings
update.check.enabled=true
update.check.on.startup=true
